# EXP-003 — Corrected adapter, one real engineering round

Date: 2026-09-17. Runtime UUID: `c241b57754f94b86a389ed20842ec9e7`.
**Measured result: one completed engineering round; independent engineering review passed.**
See the [artifact verification](../docs/verification/modal-runtime-review.md), accepted at
`384251ea79295cc8d78d1dc37b772a7959d9a236`.
Earlier failures and diagnosis remain in [EXP-001](EXP-001.md) and [EXP-002](EXP-002.md).

## Frozen execution

- Source: `1a03f77519439c0601a83c8c3b4670ca896de337`; independent actual CPU
  acceptance: `62b0fad474a0a0eafdb8a0401f67ceb1abdb92e9`.
- Image: `im-kTX4rgt7qqc1JzkmhM1vxb`; canonical CPU receipt SHA-256:
  `bdb8fd441b19d646d4c85d574ad0b10b5ee5c5f98146268c3172963175473348`.
- Remote Python 3.11.12 / 96-package environment fingerprint:
  `1ca1ef0d869fd49340cbe1cfdb8ade6f3e016cfa900443a11f60c6084a98cf14`.
- Model/processor: `Qwen/Qwen3-VL-4B-Instruct`, revision
  `ebb281ec70b05090aa6165b016eac8ec08e71b17`.
- READ2016/Bozen 1.2.0, original 350 TRAIN / 50 VALIDATION pages, source manifest
  `ac0a02a218a2142c6ee92ae964e7828ddfc65545dd6ca12f695815bb4493bdd6`.
  Original labels/splits are unchanged. Unknown document groups are admitted only
  for engineering checks; no final-test data is used.
- Reused model/image bundle:
  `94ed4f7f79d5d1110b7454c12a3b86d643cdb9dc0dece83d63734c2e4d87dcb6`.
  Local hashing and provider inventory checks passed without reupload.
- Settings SHA-256:
  `6dc4d9705888ab05380d996362da1868e0d702d9ee580c30d573b09f53dbb63d`.
  Seed 824, random selection, batch/page budget 2, maximum one round; unchanged
  cumulative reset LoRA, three epochs / three intended updates. Selected TRAIN
  pages: Seite0286 / Seite0332. Fixed validation: Seite0355 / Seite0356.
- Recipe `qwen3-vl-read-engineering-v1`, training `qwen3-vl-page-lora-v1`,
  decoding `qwen3-vl-page-greedy-v1`, evaluator `page-text-nfc-v1`.
- App `ap-QRdXZhxlhTU2U98vxJhObA`, Function `fu-ehsqesMdSYIJmEIUsMEFSj`;
  observed L40S / 2 CPU / 32 GiB, max 1/min 0/buffer 0/retries 0, execution/startup
  timeouts 600/300 seconds. Absolute run deadline: 06:05:07 UTC.

Local and provider preflights passed. The previous terminal failed call and idle
deployment were verified before the authorized replacement; both Volumes and old
run settings, deadlines and journals are preserved. The pre-run reserve was
USD 4.517 including prior usage, future compute and storage, within the gross
USD 5 first-run / USD 30 total authorization. Billing remains provisional.

This is a two-page engineering exercise. Revealed pages are a simulated label
budget, not measured annotation time; no active-learning benefit can be inferred.

## Measured result

The CLI completed in 613.011 seconds (05:24:51.993–05:35:05.004 UTC). Baseline,
selected-label fit, fresh-child checkpoint reload and post-fit validation completed.
Exactly one round committed: two revealed TRAIN pages, 348 remaining, stop reason
`page_budget`. Four provider operations completed; no retry or extra round occurred.

Training completed three updates across three epochs, with 13,005 supervised tokens.
All 144 adapter tensors changed and frozen-base checks passed. Six finite losses
were 1.532149, 1.406864, 1.389118, 1.517770, 1.360519 and 1.500381;
gradient norms were 0.460570, 0.398273 and 0.304576. Each prompt used 812 tokens;
targets including EOS used 2,206 / 2,129, both above the unchanged 2,048 decode cap.

| Artifact | SHA-256 (checkpoint IDs use `checkpoint:sha256:`) |
| --- | --- |
| Base checkpoint | `e783f52809eeda0fe459856243c8b84a6f4a952bce49ff4af762b00b2a0a2b09` |
| Adapter checkpoint | `b726e06ce06de3e052de914461dd3ec0b644b59249fce58a3fa9040b6dc1d660` |
| Adapter weights, 23,614,424 bytes | `781dc094650b5ef7b580ff09717210b3b6daf0e994c7cd8fd6f3d8ae61168e52` |
| Adapter configuration | `496cf84d5101da2169d005a91ab2f313c9100120b832fe1a6121359db2a3e7c5` |
| Derived result summary | `a474d49715d32634c3e1b22992041c40f7cc3ba68df8aa4ed0dfb0a38dc388ab` |

Reload used distinct processes (4 → 29), with exact adapter tensors, generated IDs,
status and regions. Maximum absolute probe-logit difference was 0.0, within
rtol 0.001 / atol 0.01. The selected-TRAIN probe was itself truncated; reload
agreement establishes checkpoint consistency, not transcription quality.

| Validation metric | Baseline | Post-fit |
| --- | ---: | ---: |
| Pages / failed outputs | 2 / 2 | 2 / 2 |
| Invalid output / truncated | 1 / 1 | 1 / 1 |
| Character edits / reference characters | 981 / 981 | 981 / 981 |
| Word edits / reference words | 151 / 151 | 151 / 151 |
| CER / WER (both defined) | 1.0 / 1.0 | 1.0 / 1.0 |

Seite0355 produced `invalid_output`; Seite0356 produced `truncated`, both before
and after fitting. Empty accepted region lists explain the error counts. There
is no usable validation OCR or measured improvement in this engineering run.

All operations used NVIDIA L40S, CUDA 13.0, driver 580.95.05. Model telemetry:
base load 28.017 seconds / 8,875,651,584 peak allocated bytes; baseline prediction
72.631 seconds / 9,335,729,664 bytes; fit before fresh reload 148.407 seconds /
15,119,284,224 bytes; post-fit prediction 102.957 seconds / 9,359,336,448 bytes.
These timings cover their model stages, not total provider lifecycle or billed time.

## Reopen, exports and reproduction

Fresh-process status → completed resume → status returned identical run, control
and operation records, including all four call IDs. Resume submitted no new
model operation. `results.json`, two-row `rounds.csv` and `validation.json` agree
on round membership, two-page budget, checkpoint IDs, predictions and all metrics.
All completion, worker and result artifact sizes/hashes were checked against the
durable output readback. Old failed runs, journals and indexed evidence were unchanged.

From the pinned checkout and frozen client environment, the successful sequence was:

```sh
export PYTHONPATH=src
RUN_ID=c241b57754f94b86a389ed20842ec9e7
python -m active_ocr.entrypoints.cli real preflight "$RUNS" "$RUN_ID" "$SETTINGS" --deployment "$DEPLOYMENT" --provider
python -m active_ocr.entrypoints.cli real run "$RUNS" "$RUN_ID" "$SETTINGS" "$DEPLOYMENT"
python -m active_ocr.entrypoints.cli real status "$RUNS" "$RUN_ID"
python -m active_ocr.entrypoints.cli real resume "$RUNS" "$RUN_ID" "$SETTINGS" "$DEPLOYMENT"
python -m active_ocr.entrypoints.cli real status "$RUNS" "$RUN_ID"
python -m active_ocr.entrypoints.cli real export "$RUNS" "$RUN_ID" "$EXPORT"
```

Here the variables identify this run's exclusive store, canonical runtime settings,
observed deployment and new export directory. Exact resolved commands/configuration
are retained with the indexed artifacts. Repeating the experiment requires a new
run UUID/store/settings and the same frozen inputs via the
[deployment procedure](../docs/PROJECT_GUIDE.md#8-setup-and-operating-procedure); prior frozen runs remain intact.

## Cost and final state

Provider inventory showed zero active containers; the deployment has minimum zero.
Provisional run App cost was USD 0.35677159 (L40S 0.30240138, CPU 0.01467034,
memory 0.03969987). Latest aggregate usage was USD 0.99 metered / USD 0 billed
after credits. These lagged figures are not settled bills; storage remains unknown
and the existing storage reserve is retained. Independent review can inspect the
completed artifacts without another training run.
