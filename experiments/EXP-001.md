# EXP-001 — First real OCR round: base operation timed out

Date: 2026-09-17. Status: FAILED on the first remote operation.
Runtime UUID: `b567210090044847965952f4880e5fc2`.

The intended engineering check is one baseline, one random two-page acquisition,
reset LoRA fitting, fresh-process checkpoint verification and post-fit validation.
The initial local preflight rejected the four-image input bundle. After an explicit
upload-scope correction, full-source preflight passed and the first remote base
operation timed out. No baseline, training, checkpoint, committed round or OCR
measurement exists. Actual model loading was not established.

## Frozen configuration

- Execution source: `1a413d7e373a1182c3552bd9c05c9687fef14485`;
  independent actual CPU review: `fb67890aac1bfe6b7e7c21f4c117dc24a076a729`.
- Accepted image: `im-akVzgVJvRAXoqB65yowGeN`; CPU receipt SHA-256:
  `23bcabdd3fe6c0a89a36c31b09c9e21e24010bec2c8e9b74558e07d4ea7dc069`.
- READ2016/Bozen 1.2.0, original 350 training / 50 validation pages;
  source manifest SHA-256:
  `ac0a02a218a2142c6ee92ae964e7828ddfc65545dd6ca12f695815bb4493bdd6`.
  Unknown document groups admitted only for engineering verification;
  original labels/splits preserved, no final-test data.
- Model and processor: `Qwen/Qwen3-VL-4B-Instruct` revision
  `ebb281ec70b05090aa6165b016eac8ec08e71b17`;
  recipe `qwen3-vl-read-engineering-v1`, training policy
  `qwen3-vl-page-lora-v1`, decode policy `qwen3-vl-page-greedy-v1`.
  See [runtime recipe](../docs/qwen-runtime.md) for precision, LoRA and token limits.
- Random seed 824, batch/page budget 2, maximum one round, reset cumulative fit,
  three epochs / three updates intended. Expected selected training pages:
  `read2016-v1.2.0:train:Seite0286`, `read2016-v1.2.0:train:Seite0332`.
  Fixed validation pages: `read2016-v1.2.0:validation:Seite0355` and
  `read2016-v1.2.0:validation:Seite0356`; evaluator `page-text-nfc-v1`.
- Local Python 3.11.14, Modal 1.5.5; local dependency SHA-256:
  `98e5e21932097f115407f8f5934b210f6cb2fb76dda262f57dc12638c4499c4a`.
  Initial canonical runtime settings SHA-256:
  `1fff82cb13f62f0733a87d70a21284c0f9ac828995e1d934fbb7f152f8bf0c82`.
- Initial uploaded bundle: 12 required model/processor files and four page images,
  8,892,168,931 bytes; bundle SHA-256:
  `fb46020abd065e4ac22525415e5852f9d42cf6813b33f4ab1b5945077d143d4d`.
  Local bytes were rehashed against pinned identities before upload. Provider listing
  records the expected files and sizes; no remote model loading/hash verification occurred.
  No oracle, source XML, labels or database was uploaded.

## Initial preflight failure and reproduction

Run creation through `Pipeline.create_simulation(..., kind=RunKind.REAL)` froze
the full source and generated the UUID above. With this run, the recorded settings
and exact client environment, the existing CLI command is:

```sh
PYTHONPATH=src python -m active_ocr.entrypoints.cli real preflight \
  "$RUNS" b567210090044847965952f4880e5fc2 "$RUNTIME_SETTINGS"
```

At the execution source, `ModalModel.check_run` in
`src/active_ocr/integrations/modal_model.py:806` constructs the required image set
from **all 400 frozen source pages**, then rejects the four-image bundle with
`ValueError: input bundle must contain exactly the frozen source images`.
The later image-size loop also assumes every source image is bundled.
This conflicts with the approved four-image upload scope. The failure is local
and occurs before transport creation or a paid model call.

This run and immutable input bundle were preserved. The subsequent release corrected
upload scope to match the existing full-source image contract. The original source,
fixed training/validation selection and held-out-label isolation stayed intact;
no production change or new CPU build was made.

## Authorized continuation and remote timeout

Before changing settings, the store had no baseline, rounds, model control or model
operations, and no deployment existed. Revised settings were written separately;
all initial evidence files remain unchanged. The same UUID was retained.

The corrected bundle contains 12 pinned model/processor files and all 400 original
TRAIN/VALIDATION images, totaling 9,380,678,920 bytes. Only the same four fixed pages
were eligible for the planned model operations. Local bytes were rehashed, local
CLI preflight passed **before** upload, and the provider listing matched all 412
file names/sizes. Neither bundle contains XML, labels, oracle or database files.

- Full bundle SHA-256:
  `94ed4f7f79d5d1110b7454c12a3b86d643cdb9dc0dece83d63734c2e4d87dcb6`.
- Revised settings SHA-256:
  `68449fdbfa218cc1b3a6cf02d0bee2e1108db231f7dc08e6d4f4d68f19a7fc51`.
- App: `ap-fIGoOjBJ1tjBOtRx6Kg9FV`;
  dispatcher: `fu-t9JSbbTLwL7MfTjI9uXTEG`.
- First and only call: `fc-01M2PNSM04FHPNHR01986207F9`;
  operation: `9e7eb2ba20d8d6126bfef73f742ebd571ffce052665c2dcd10f50085b4ca0c6b`.

Provider-returned configuration verified the accepted image, read-only bundle mount,
separate writable UUID subtree, one L40S, 2 CPU / 32,768 MiB limits, maximum one
container, zero warm/buffer containers, no retries, two-second scale-down, 600-second
execution timeout and 300-second startup timeout. Provider CLI preflight also passed.
The unchanged full-run command was:

```sh
PYTHONPATH=src python -m active_ocr.entrypoints.cli real run \
  "$RUNS" b567210090044847965952f4880e5fc2 \
  "$REVISED_RUNTIME_SETTINGS" "$DEPLOYMENT_OBSERVATION"
```

The first-submission deadline was fixed at 2026-09-17 03:55:34 UTC. The provider
terminated the base operation at 03:25:40 UTC with `FunctionTimeoutError` after
600 seconds; its call graph reports `TIMEOUT`. The coordinator exited with code 1
and retained the operation as `UNKNOWN` with its known call ID and active lock.
Reading that existing call confirmed the terminal timeout; no resubmission or
state override was attempted.

The run output subtree was empty after termination. Logs contain cancellation and
the redacted `dispatch_failed` exception, but no diagnostic stage or model output.
There is no durable run-control, worker manifest, checkpoint or completion/failure
receipt. The first durable commit follows input verification in the implementation;
the exact internal timeout cause remains **unknown**. Do not infer OOM, successful
model loading, or a need for a longer timeout from this evidence.

The failed run has zero committed baseline/rounds and no revealed training labels.
Fresh-process local inspection and provider reconciliation succeeded. Completed-run
resume, checkpoint reload and result exports were not reached. Subsequent work
requires bounded diagnosis and a new release; the script stopped on the first error.

## Bounded CPU diagnosis

One separately authorized diagnostic reused the accepted image and read-only full
bundle, with 2 CPU / 4 GiB, no GPU, 180-second Function timeout and no retries.
It used a new synthetic output subtree; the original run and all indexed evidence
were checked unchanged afterward. App `ap-TbkpMPrXlN5WETGwOv8Stw`, call
`fc-01M2PPXHAX1QMZTMV7669Y5W6K`, result SHA-256
`0df6a0747e1cdde3bfa27eb99e2bfbc6a37f4b9f5c1ce7749a3c192a61cf09df`.

The diagnostic recorded entrypoint/settings checks, SDK access, commit-handle
construction and root checks completing within 0.856 seconds of child startup.
It then called the unchanged `_files` helper separately for each inventory entry
to record start/end timestamps. In 139.537 seconds of file verification, 269 image
files totaling 331,833,179 bytes passed. Median per-file time was 0.174 seconds;
range 0.097–2.869 seconds. All image names sort before model files.

The 140-second watchdog captured a stack in `qwen.py:151`, inside `file_hash`'s
file read, called by `modal_app.py:115` in `_files`. The child was terminated and
reaped; total parent duration was 143.432 seconds. At termination, ordinal 270
had started but not completed. Directory inventory, worker publication, synthetic
publish/commit and model loading were not reached. The diagnostic output subtree
was empty, and the ephemeral App stopped with zero containers afterward.

This **measures a slow full-input verification boundary**. It does not identify
the exact original GPU timeout stage or measure model-shard throughput. The setup
ran in a fresh child, not the original provider dispatcher thread; SDK context,
memory allocation and hardware differ. No file-integrity failure was observed
among completed checks. No ML package/model invocation was added.

The smallest proposed correction is to retain the full immutable bundle, inventory
and symlink checks while hashing only requested image bodies in the parent
dispatcher (none for base loading). Existing child checks must still hash pinned
model/processor assets before loading and validate every consumed image. This is
a recommendation, **not implemented**. A separately authorized model-shard-only
CPU timing would resolve the remaining I/O uncertainty before another GPU attempt;
image timings cannot reliably predict large-shard performance.

## Usage and limitations

Planned maximum: one L40S dispatcher, zero warm containers, original 40-minute
envelope and USD 5 first-run / USD 30 aggregate ceiling, including prior CPU setup.
Losses, CER/WER, prediction failures, VRAM and checkpoint reload are unmeasured.
After the timeout the provider listed zero containers; the dispatcher remains
deployed with minimum zero containers. No additional call was made.

Before upload, provider accounting reported USD 0.01865657 aggregate compute
usage from earlier CPU setup; rounded metered total USD 0.02, billed USD 0 after
credits. These observations are provisional, not settled billing or a guarantee
of free storage. Uploaded storage continues to exist; no useful artifacts were deleted.
After the failed GPU call, the 03:26 UTC summary reported USD 0.45 metered / USD 0
billed after credits, including USD 0.43072408 for deployed Apps. The contemporaneous
resource report for this App totaled USD 0.39238034 (CPU 0.01613456, L40S 0.33258353,
memory 0.04366225). These disagree because accounting is provisional; neither is a
settled bill. Retain the earlier CPU observation rather than treating its later
lower summary as a refund. Storage was not yet metered. The revised conservative
estimate, including 24 GiB-month retention and original compute reserves, was
USD 3.974150 under the first USD 5 ceiling; this is an estimate, not actual usage.

The CPU diagnostic resource report adds USD 0.00519754 (CPU 0.00388379, memory
0.00131375), provisional. Its later aggregate summary was USD 0.41 metered / USD 0
billed, lower than the prior USD 0.45 observation; both are retained because
accounting remains unsettled. No refund or final total is inferred.

Private canonical configurations, allowlists, upload listings, command outputs and
SQLite state are retained under restricted operational storage; request access
from the project operator. Research interpretation: NOT_STARTED. This attempt
provides no evidence of OCR quality or active-learning improvement.
