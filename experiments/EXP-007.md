# EXP-007 — stronger Qwen3-VL recipe quality gate

19 September 2026 UTC. Run `0207891673914fffb3bad2902218ec20`, source `96e59d850f51a8d2e6d903af4d096ee38995a51f`, recipe `read2016-llamafactory-joint-v2` SHA-256 `b0f9025742224bb6dcf9beb943690baeb9d6944102b975853fec0070b53d64a4`. The new image passed all five actual Linux CPU checks. The READ2016 official split still has unknown document grouping; this is engineering calibration, not document-independent evidence.

The run predicted eight fixed VAL pages (`Seite0355`–`Seite0362`) with the untouched base model before revealing any labels. It then selected 64 TRAIN pages by seeded random acquisition, reset-fit LoRA for three epochs (48 optimizer updates), and predicted the same VAL pages. The v2 changes from EXP-006 were 2,097,152 image pixels, LLaMA-Factory's `lora_target=all`, learning rate 1e-4, cosine scheduler and warmup 0.1. Every selected target passed the actual toolkit-encoded length preflight. The trained checkpoint passed fresh-process reload and had 252 changed LoRA B tensors. Its final logged losses fell to about 1.02–1.34, versus roughly 1.3–1.6 in the previous recipe.

| Fixed eight VAL pages | Untouched base | After 64 TRAIN pages |
| --- | ---: | ---: |
| Valid / invalid / truncated outputs | 0 / 1 / 7 | 0 / 5 / 3 |
| CER / WER | 1.0 / 1.0 | 1.0 / 1.0 |
| Matched / reference line boxes | 0 / 182 | 0 / 182 |
| Box F1 | 0 | 0 |

Raw outputs after training still repeat `regions` entries, invent boxes with identical corners, or reach the 4,096-token cap. Better optimization did **not** yield usable OCR. The planned three-round/192-page run was deliberately stopped after its first quality gate; no second or third acquisition occurred. This is a failed OCR recipe, not a completed active-learning comparison.

The frozen database, build receipt, settings, raw prediction receipt and checkpoint summary are retained under `.local/verification/exp-010/attempt-1/` and in the Modal output Volume. The deployment was stopped; prior checkpoints and failed outputs were preserved. A simpler model response target is the next minimal change; source labels, membership and evaluator must remain unchanged.
