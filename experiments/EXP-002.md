# EXP-002 — Corrected real OCR round

Date: 2026-09-17. Status: FAILED during fit; baseline committed, no completed round.
Runtime UUID: `add38223387e44588834d3c49d1a756a`.
The failed [first attempt](EXP-001.md), including its original run/control, is preserved.

## Frozen execution

- Source `7dd40ec6cc444c939bdd09b42963bbf639cecbca`; independent actual CPU
  acceptance `1fb7404ab0579f2c99f68715b6258babaeee956c`.
- Accepted image `im-82wRAkCIoH38W1LnO3G4KT`; CPU receipt SHA-256
  `cb477825e80734235813a86eeb98309455e814ba3a19a02ba185fc19609cc550`.
- Full READ2016/Bozen 1.2.0 original 350 TRAIN / 50 VALIDATION source, manifest
  `ac0a02a218a2142c6ee92ae964e7828ddfc65545dd6ca12f695815bb4493bdd6`.
  Unknown document groups are admitted only for engineering checks. Labels and
  official splits are unchanged; no final-test data is used.
- Existing model/image bundle
  `94ed4f7f79d5d1110b7454c12a3b86d643cdb9dc0dece83d63734c2e4d87dcb6`
  was reused after local hashing and exact provider file/size inventory checks.
  No reupload occurred; the bundle contains no oracle, XML, labels or database.
- Model/processor `Qwen/Qwen3-VL-4B-Instruct` revision
  `ebb281ec70b05090aa6165b016eac8ec08e71b17`, unchanged
  `qwen3-vl-read-engineering-v1` recipe. Seed 824, RANDOM, batch/page budget 2,
  maximum one round, cumulative reset LoRA, three epochs / three updates intended.
- Expected TRAIN selection: `read2016-v1.2.0:train:Seite0286` and
  `read2016-v1.2.0:train:Seite0332`. Fixed validation: Seite0355 / Seite0356
  in the original validation split, baseline and post-fit evaluator `page-text-nfc-v1`.
- Canonical runtime settings SHA-256
  `ff9ebd0b8f4ae947a2857168ce4f878b9269305f2db969052d772f45dff81e98`.
  New App `ap-iopNdmR2A4lJNGedhAslBB`, Function `fu-MpaKzqrRgyezWKreUhguuk`.

Local/provider preflights passed. The prior call's terminal timeout and zero active
containers were reconfirmed before its explicitly authorized deployment replacement.
Provider-returned configuration matched the accepted image, read-only bundle and
new UUID output subtree, one L40S / 2 CPU / 32 GiB, max 1/min 0/buffer 0, retries 0,
600-second execution / 300-second startup timeout and two-second scale-down.

The first-submission deadline is fixed at 04:56:40 UTC; no extension or automatic
retry is allowed. Gross USD 5 first-run / USD 30 total limits include prior failures
and storage. The conservative reserve estimate was about USD 4.44; actual billing
remains provisional. This engineering run cannot establish active-learning benefit
or measured human annotation time.

## Observed execution

The recorded invocation, from the clean pinned checkout with the unchanged
Python 3.11.14 / Modal 1.5.5 client, was:

```sh
PYTHONPATH=src python -m active_ocr.entrypoints.cli real run \
  "$RUNS" add38223387e44588834d3c49d1a756a \
  "$RUNTIME_SETTINGS" "$DEPLOYMENT_OBSERVATION"
```

Base loading completed (`fc-01M2PS9FG5QQCY5YAHT70R9CDE`) and produced verified
checkpoint `checkpoint:sha256:8499bbe72872241e4b9e33f2269cd1c296f51e9a400e6413095837a3bcc03052`.
Measured telemetry: NVIDIA L40S, CUDA 13.0, driver 580.95.05, 32.171 seconds within
the model operation and peak allocated memory 8,875,651,584 bytes. This establishes
actual pinned-model loading on CUDA for this scope.

Baseline prediction completed (`fc-01M2PSAXWWV1G5VT3TEMBK5CQQ`) and committed
with zero revealed training pages. Both OCR predictions failed the output contract:
Seite0355 was `invalid_output` (EOS); Seite0356 was `truncated` (length limit).
The evaluator used their empty accepted region lists. These are recorded failures,
not usable OCR outputs or an accuracy improvement.

| Baseline metric | Observed |
| --- | --- |
| Validation pages / failed pages | 2 / 2 |
| Invalid format / truncated / refusal | 1 / 1 / 0 |
| Character edits / reference characters | 981 / 981 |
| Word edits / reference words | 151 / 151 |
| CER / WER | 1.0 / 1.0 |
| CER defined / WER defined | true / true |

Baseline model-operation time was 100.702 seconds; peak allocated memory was
9,335,729,664 bytes. The subsequent fit request contained exactly the two expected
TRAIN pages and their original revealed labels. Only selected training targets
were sent transiently; validation truth stayed with the local evaluator.

Fit call `fc-01M2PSF0ZF1FQX2HQD738G64SJ` failed at 04:20:34 UTC. The provider call
graph reports `FAILURE`, and reading that same call returns
`WorkerError: dispatch_failed`. A durable worker manifest and failure receipt
exist for operation
`d7396d247e9de2bb779b658fcea12d64bded99e0b1b9ed5d11336a37696a0a42`;
the receipt's bounded code is `model`, SHA-256
`3749d52d97945624845ce9f22a76d7dd14004cfd2642a75a95ec9ec76bbe0e25`.
The child stderr was not retained by the existing runner. The underlying failure
cause, completed update count, losses and fit memory are therefore **unknown**;
neither OOM nor a token-limit failure is established.

The coordinator exited 1 and retained the fit journal as `UNKNOWN` with its known
call ID and active lock. There was no retry, deadline extension or manual state
override. No adapter checkpoint, reload probe, post-fit evaluation or round commit
exists. Completed-run resume and JSON/CSV export were not reached. Two labels
were revealed for the attempted fit, but no successful round is committed.

## Reconciliation and next gate

After failure, the provider listed zero containers. The new deployment remains
configured with zero warm containers. The old run and every previously indexed
artifact were independently checked unchanged. Private records retain canonical
configuration, exact scripts/commands, provider observations, baseline receipts,
checkpoint, failure evidence, local state and artifact hashes. Derived result
summary SHA-256:
`8e44fcb69c51f3e7ce84f80f0cb802ba01509c38001421ba6a9bf04ac580cc3c`.

The immediate App resource report totals USD 0.13585467 (L40S 0.11515110,
CPU 0.00558630, memory 0.01511727). The aggregate summary reported USD 0.57 metered
and USD 0 billed after credits. These are provisional, potentially lagged figures;
storage and settled billing remain unknown. No extra diagnostic or model call
was launched after the failure.

The full-round acceptance criteria remain unmet. The next gate is bounded diagnosis
of the retained fit failure and independent review of any correction before a new
execution release. Research interpretation: NOT_STARTED; no claim of active-learning
improvement or reduced human annotation is supported.


## CPU diagnostic: setup failure

One CPU-only invocation used the accepted image and read-only bundle, 2 CPU / 4 GiB,
no GPU/retries, a 300-second limit and 280-second child watchdog. An earlier setup
assertion rejected the provider's empty GPU configuration before any invocation;
that setup is preserved. The sole call `fc-01M2PTGSN3AWJJGEEKA8T33T0Z` in App
`ap-KNvOELseunyiHjFgBwMGop` then failed after source/package checks because the
probe omitted production's input-root `resolve(strict=True)`. Processor inventory
rejected the mount symlink. The child exited 1 and was reaped after 0.607 seconds.

Target serialization/encoding and model loading were not reached. This is a probe
setup error; the original fit cause remains unknown. No retry occurred. Both Apps
stopped, zero containers were observed, and the original run state/all 59 indexed
artifacts remained unchanged. No target file/log was created. Diagnostic cost rows
were not posted (cost unknown; estimate below USD 0.02). Result-summary SHA-256:
`d20571b468a6bae791700f77c73b09af4a0652073c65e327cefceb589075e813`.
